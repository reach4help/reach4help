/* eslint max-len: 0 */
import { PartialLanguage } from '../iface';
import strings from './tr.json';

const LANG: PartialLanguage = {
  meta: {
    name: 'Türk',
    direction: 'ltr',
  },
  strings,
};

export default LANG;
