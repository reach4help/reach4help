/* eslint max-len: 0 */
import { PartialLanguage } from '../iface';
import strings from './pt.json';

const LANG: PartialLanguage = {
  meta: {
    name: 'Português',
    direction: 'ltr',
  },
  strings,
};

export default LANG;
