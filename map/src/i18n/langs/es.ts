/* eslint max-len: 0 */
import { PartialLanguage } from '../iface';
import strings from './es.json';

const LANG: PartialLanguage = {
  meta: {
    name: 'Español',
    direction: 'ltr',
  },
  strings,
};

export default LANG;
