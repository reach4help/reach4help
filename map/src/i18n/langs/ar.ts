/* eslint max-len: 0 */
import { PartialLanguage } from '../iface';
import strings from './ar-SA.json';

const LANG: PartialLanguage = {
  meta: {
    name: 'العربية',
    direction: 'rtl',
  },
  strings,
};

export default LANG;
