/* eslint max-len: 0 */
import { PartialLanguage } from '../iface';
import strings from './ar.json';

const LANG: PartialLanguage = {
  meta: {
    name: 'العربية',
    direction: 'rtl',
  },
  strings,
};

export default LANG;
