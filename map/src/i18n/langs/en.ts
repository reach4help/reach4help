/* eslint max-len: 0 */
import { Language } from '../iface';
import strings from './en.json';

const LANG: Language = {
  meta: {
    name: 'English',
    direction: 'ltr',
  },
  strings,
};

export default LANG;
