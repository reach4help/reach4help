import { LANGUAGE_PREFERENCE_LOCALSTORAGE_KEY as localStorageKey } from './localStorageKeyPrefix';

const supportEmail = 'support@reach4help.org';

export default {
  localStorageKey,
  supportEmail,
};
