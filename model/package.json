{
  "name": "@reach4help/model",
  "version": "0.0.1",
  "scripts": {
    "lint": "eslint . --ext .ts",
    "build": "rm -rf lib && tsc --build src/tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^3.8.3"
  },
  "dependencies": {
  }
}
